# 学习日志 · 故事-T 恤设计生成器

> 实习项目 
> 目标：从零做一个「故事文本 + 参考图 → T 恤设计图」AI Web 应用

---

## Day 1（上午） · 读懂需求、选型、搭通第一条链路

**读任务书花的时间比我预计的长。**

第一遍读完以为核心任务就是"叫 AI 画四张图"。真正开始想实现的时候才发现，最难的问题是**风格一致性**——图像 API 的每一次调用都是独立的随机过程，什么都不做的话，4 张图可能出来 4 种完全不同的画风。这正是任务书里"难点 2"提到的。

**围绕这个问题研究了几个方案：Midjourney 的 `--sref`（行业公认效果最好，但没有官方 API）、ComfyUI 的 IP-Adapter（需要本地部署，太重）、通义万相的 `ref_mode="style"`（有官方 Python SDK，文档是中文，API 参数直接支持）。最终选通义万相，不是因为它效果最好，而是它对新手最友好、出问题最容易排查。**

**技术选型最终结果（三个模型，同一个平台，只需一个 Key）：**

- `qwen-vl-max`：读参考图，输出风格描述（40字内）
- `qwen-plus`：读故事，输出 4 个版位的图像 Prompt（JSON 格式）
- `wanx2.1-t2i-turbo`：把 Prompt + 参考图 → 生成 4 张图

一开始以为要注册两三个平台、管多个 Key，后来发现这三个模型全在阿里云 DashScope 下，同一个控制台、同一个 Key——这个发现省了不少配置时间。

**环境搭建踩了两个坑。**

第一个：装 Python 没勾 "Add python.exe to PATH"，装完在终端输 `python --version` 报找不到命令。折腾了 20 分钟，重跑安装包点 Modify 补上。教训：安装界面的每个勾选框都要读一遍，不要直接 Next。

第二个：PowerShell 跑 `.venv\Scripts\activate` 报"无法加载，因为在此系统上禁止运行脚本"。这是 Windows 的执行策略默认限制 PowerShell 脚本。解决方法：`Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`。这个错误网上一搜就有，但第一次遇到确实懵了一下——报错信息里完全没有提示怎么解决。

**故事拆解模块（`story_parser.py`）的核心难点是让 LLM 输出稳定格式。**

第一次写的 system prompt 只说"把故事拆成 4 个版位的描述"，LLM 每次返回的结构都不一样，有时候是列表、有时候是段落，代码解析不了。

改进方向是：在 system prompt 里直接贴出期望的 JSON 结构，并且明确说"只输出 JSON，不允许任何额外解释"。加上 `result_format="message"` 参数后，输出终于稳定。

另外，LLM 返回的文本有时候会带 ` ```json ``` ` 代码块标记，直接 `json.loads()` 会报错。在 `story_parser.py` 里加了一段预处理，把带 ` ``` ` 的行过滤掉再解析。这个问题很常见，但第一次遇到完全没想到。

---

## Day 1（下午）三大功能模块开发

1.**故事拆解模块（`story_parser.py`）**

核心难点是让 LLM 输出稳定格式。

第一次写的 system prompt 只说"把故事拆成 4 个版位的描述"，LLM 每次返回的结构都不一样，有时候是列表、有时候是段落，代码解析不了。

改进方向是：在 system prompt 里直接贴出期望的 JSON 结构，并且明确说"只输出 JSON，不允许任何额外解释"。加上 `result_format="message"` 参数后，输出终于稳定。

另外，LLM 返回的文本有时候会带 ` ```json ``` ` 代码块标记，直接 `json.loads()` 会报错。在 `story_parser.py` 里加了一段预处理，把带 ` ``` ` 的行过滤掉再解析。这个问题很常见，但第一次遇到完全没想到。

2.**参考图分析模块（`style_extractor.py`）**

把"风格"提取成一段稳定的文字,使后面 4 张图对齐。难点在于如何实现风格的一致性

调 `qwen-vl-max` 输入图片,让它输出三段:`palette`(色彩)、`composition`(构图)、`technique`(技法)。Prompt 里我强制要求 JSON 格式,**查阅相关资料，借鉴即梦的“参考图”和“角色一致性”功能**：

| 层                    | 代码位置                                                   | 作用               |
| --------------------- | ---------------------------------------------------------- | ------------------ |
| 风格锚点拼接          | `story_parser.py` 最后把 `style_anchor` 拼到每个 Prompt 前 | 语义层约束         |
| 参考图 style 模式     | `image_gen.py` 中 `ref_mode="style", ref_strength=0.5`     | 视觉层约束色调笔触 |
| 统一模型 + 统一参考图 | 所有版位调同一个模型、传同一张参考图                       | 减少随机性来源     |

测试了三张完全不同风格的图——水墨鱼、田园风光、多色拼图动漫，跑一遍看输出。第一次跑水墨鱼,返回的是 `"palette": "黑色、白色和灰色,营造出一种极简而富有禅意的水墨意境"`——能用,但一行话太长,**后面拼到生图 prompt 里遇到了爆字数的问题**。改 system prompt 强制每段不超过 30 字得以解决。

3. **文生图模块（`image_gen.py`）** 

调通义万相 `wanx2.1-t2i-turbo`,关键参数有两个:

- `ref_img`(参考图):贯穿 4 张图的视觉锚点
- `ref_strength`(参考强度):0~1,我调到 0.5——再大就抄参考图,再小就跟原图无关

四个版位的尺寸我特意分开设了:前胸 `1280×720`(横版,主视觉)、后背 `1024×1024`(方形,叙事感)、袖口 `512×512`(小图,极简)、胸章 `512×512`(章印)来模拟任务书的参数。`SIZE_MAP` 字典管理。

**第一次踩坑**:返回的 `response.output.results[0].url` 是个临时 URL,几小时后失效。所以拿到 URL 必须立刻 `requests.get` 下载到本地。这个细节官方文档藏得有点深,我是看 SDK 示例代码才发现的。

测试用一张水墨参考图 + 老虎故事,跑出来:前胸是水墨虎站姿、胸章是"真奇怪"印章字、袖口是简笔虎纹、后背是双虎呼应。**风格对得上**——两张老虎都是水墨没色调跳脱。这是今天最大的成就感时刻。

## Day 2（上午）剩余两大功能模块开发

4.**串联模块（· `pipeline.py` ）**

实现逻辑:`extract_style → parse_story → 循环 generate_image 4 次`。中间加了 `time.sleep(5)` 防止 API 限流。返回 `{"prompts": ..., "images": ...}`。

写完跑一次完整流水线,故事用命题A以及古诗词《江雪》和我自己随意写的一段文字来进行测试。生成效果基本满足条件。

5.**贴图模块（`mockup.py`）** 

开发这个模块遇到了很多问题，同时也学习到了如何使用**PIL**及**numpy库**处理图片。

**问题一：**函数签名 `create_mockup(tshirt_path, design_path, output_path)`——只贴单张图。测试脚本里我已经按"贴 4 张"写了,直接 `TypeError: create_mockup() got an unexpected keyword argument 'image_paths'`。

通过读取报错信息得知 `image_paths` 不认识,翻译过来就是函数定义里没这个参数。两条路:要么改函数让它认这个参数,要么改调用方法。我选了改函数(因为本来就该支持多图)。

**问题二:坐标完全错位**。改完函数能跑,贴出来一看袖口图飞到胸前、胸章和前胸大图重叠。一开始用 alpha 通道自动检测 T 恤边界——但**我的 T 恤模板是白底不是透明底**,bbox 算出来是整张图的矩形,坐标算偏。

为了解决这个问题，我编写了校准工具 `**calibrate_mockup.py**`:在 T 恤图上画三个不同颜色的圆点,改比例数字,运行,肉眼看圆点是不是落在前胸/胸章/袖口的位置。三轮就调到位:

```python
SPOTS = {
    "前胸 front":  (0.50, 0.51, 40),   # 胸口正中
    "胸章 badge":  (0.60, 0.30, 20),   # 左胸口袋位
    "左袖 sleeve": (0.25, 0.35, 15),   # 左袖中间
}
```

校准工具花了 20 分钟写,但省了大量盲调。

**问题三：贴图边缘有白色方块**。AI 生成的图自带白底,直接贴上去边缘有明显白方块,丑。一开始想的是"把白色像素变透明"——硬阈值切完一看,边缘锯齿。原因是 AI 出图背景不是纯白,而是 245~252 的渐变白。

改成**渐变透明 + 颜色补偿**两步:像素越接近白 alpha 越低,半透明区域颜色再往 T 恤底色拉一拉,消除色边。代码里关键就这两行:

```python
new_alpha = np.clip((255 - min_channel) / (255 - threshold) * 255, 0, 255)
arr[:, :, :3] = arr[:, :, :3] * (1 - blend * 0.5) + bg_arr * blend * 0.5
```

**问题四:背景不一定是白色**。等真用上深色 mockup 模板时,前面"假定背景是白色"的方案就崩了。最终方案:**取设计图四角像素的中位数当背景色**——因为 AI 出图主体一般在中央,四角大概率是背景;用中位数而不是平均值,能抗住"某个角恰好溢出图案"的干扰。然后按颜色欧氏距离做渐变透明。这是代码层面思考最久的一段。

## Day 2（下午）前端界面开发（app.py）

学习了基于Streamlit构建 ——`st.text_area` 输入框、`st.file_uploader` 上传图、`st.columns(2)` 双列布局。

**问题一:`session_state` 不存东西**。第一次跑完一次生成,点别的按钮页面就刷新,所有图全没了。Streamlit 默认每次交互整个脚本重跑,变量都重置——必须把生成结果塞进 `st.session_state` 才能跨刷新存活。

**问题二:`IndexError: list index out of range`**。这个报错让我当场懵了几分钟。代码 `response.output.results[0].url` 看着没毛病。冷静下来想:这个错只可能是 `results` 是空列表 `[]`。但 API 调用没报错说明请求过了——那只能是**通义万相内容审核拦了 prompt,但只返回空结果不返回错误码**。

排查方式很笨但有效:在取 url 之前先 `print(response)`,看 API 到底返回了啥。果然 `code: 'DataInspectionFailed'`。这个排查思路可以总结成一条经验:**当代码看着没问题但报"空"错误时,先打印上一步的完整数据**。将 prompt 中的敏感词换掉就好了。

将上述问题都解决完以后，开始整理交付文档、视频的录制和上传代码。











